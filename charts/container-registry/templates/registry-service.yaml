apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-registry
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 443
      protocol: TCP
      name: https
  selector:
    app: {{ .Release.Name }}-registry
